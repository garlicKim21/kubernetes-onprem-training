apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
spec:
  type: NodePort
  selector:
    app: nginx-svc-demo
  ports:
    - protocol: TCP
      port: 80            # Service 포트 (ClusterIP로 접근 시)
      targetPort: 80      # Pod의 컨테이너 포트
      nodePort: 30029     # 노드 포트 (30000-32767)
