apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_ENV: "production"
  APP_PORT: "8080"
  app.properties: |
    database.host=db.example.com
    database.port=5432
    cache.ttl=300
